import BaseButton from './BaseButton'
import styles from './Button.module.scss'
const CompareButton = () => {
	return <BaseButton className={styles.compareButton}>比較按鈕</BaseButton>
}

export default CompareButton
